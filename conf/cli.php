; <?php /*

commands[migrate/up] = Apply the specified migration
commands[migrate/down] = Revert the specified migration
commands[migrate/list] = Print a list of available migrations
commands[migrate/current] = Print the current version for a migration
commands[migrate/is-latest] = Print whether the specified migration is up-to-date
commands[migrate/versions] = Print a list of versions available for a migration
commands[migrate/generate] = Generate a new migration for the specified class

; */ ?>